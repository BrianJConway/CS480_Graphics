
General notes:
--------------

In tutorial, shader use() function is same as our shader enable() function.






Model Class needs:
------------------

glm::mat4 model data member to store model matrix of object

Update function:
Much like the object update function, how we translate and rotate, etc.

GetModel function:
returns the glm::mat4 model

Since we decided we don't want to implement textures for this week, edit the 
prototype of the Draw() function to not have a shader parameter.





Mesh Class needs:
-----------------

Since we decided we don't want to implement textures for this week, edit the 
prototype of the Draw() function to not have a shader parameter.
